# Smart-Textiles
```sh
basic.forever(function () {
    let resistance = pins.analogReadPin(AnalogPin.P0)
    if (resistance >= 900 && resistance <= 905) {
        basic.showString("A")
    } else if (resistance > 905 && resistance <= 910) {
        basic.showString("B")
    } else if (resistance > 910 && resistance <= 915) {
        basic.showString("C")
    } else if (resistance > 915 && resistance <= 920) {
        basic.showString("D")
    } else if (resistance > 920 && resistance <= 925) {
        basic.showString("E")
    } else if (resistance > 925 && resistance <= 930) {
        basic.showString("F")
    } else if (resistance > 930 && resistance <= 935) {
        basic.showString("G")
    } else if (resistance > 935 && resistance <= 940) {
        basic.showString("H")
    } else if (resistance > 940 && resistance <= 945) {
        basic.showString("I")
    } else if (resistance > 945 && resistance <= 950) {
        basic.showString("J")
    } else if (resistance > 950 && resistance <= 955) {
        basic.showString("K")
    } else if (resistance > 955 && resistance <= 960) {
        basic.showString("L")
    } else if (resistance > 960 && resistance <= 965) {
        basic.showString("M")
    } else if (resistance > 965 && resistance <= 970) {
        basic.showString("N")
    } else if (resistance > 970 && resistance <= 975) {
        basic.showString("O")
    } else if (resistance > 975 && resistance <= 980) {
        basic.showString("P")
    } else if (resistance > 980 && resistance <= 985) {
        basic.showString("Q")
    } else if (resistance > 985 && resistance <= 990) {
        basic.showString("R")
    } else if (resistance > 990 && resistance <= 995) {
        basic.showString("S")
    } else if (resistance > 995 && resistance <= 1000) {
        basic.showString("T")
    } else if (resistance > 1000 && resistance <= 1005) {
        basic.showString("U")
    } else if (resistance > 1005 && resistance <= 1010) {
        basic.showString("V")
    } else if (resistance > 1010 && resistance <= 1015) {
        basic.showString("W")
    } else if (resistance > 1015 && resistance <= 1020) {
        basic.showString("X")
    } else if (resistance > 1020 && resistance <= 1025) {
        basic.showString("Y")
    } else if (resistance > 1025 && resistance <= 1030) {
        basic.showString("Z")
    }
})

```